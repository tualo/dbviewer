Ext.define('Tualo.dbviewer.lazy.models.Viewport', {
    extend: 'Ext.app.ViewModel',
    alias: 'viewmodel.cmp_db_viewport',
    stores: {
      /*
      typestore:{
        fields: ['id', 'name'],
        data : [
          {"id":"belegdatum", "name":"Belegdatum"},
          {"id":"buchungsdatum", "name":"Buchungsdatum"}
        ]
      }
      */
    }
});
